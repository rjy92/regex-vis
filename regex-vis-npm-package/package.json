{  
  "name": "regex-vis-web-component",  
  "version": "0.4.0", 
  "publishConfig": {  
    "access": "public"  
  },  
  "description": "Regex visualization web component for Vue and other frameworks",  
  "main": "dist/regex-vis-cn.js",  
  "types": "index.d.ts",  
  "files": [  
    "dist",  
    "index.d.ts",  
    "README.md"  
  ],  
  "scripts": {  
    "build": "vite build",  
    "dev": "vite",  
    "preview": "vite preview"  
  },  
  "keywords": ["regex", "visualization", "web-component", "vue"],  
  "author": "Your Name",  
  "license": "MIT",  
  "peerDependencies": {  
    "react": "^18.0.0",  
    "react-dom": "^18.0.0"  
  },  
  "devDependencies": {  
    "@vitejs/plugin-react-swc": "^3.7.0",  
    "vite": "^5.3.3",  
    "vite-tsconfig-paths": "^4.3.2",  
    "typescript": "^5.5.0"  
  }  
}